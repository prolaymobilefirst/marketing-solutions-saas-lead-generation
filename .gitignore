.vscode
CLAUDE.md
results-backup.html
requirement.md
/docs
/designs
/compressed
/pagespeed
css/styles.css
js/layout.js
js/quiz.js
js/webhook.js
.git-credentials.local
deploy.sh

# Secrets (PDF_TOKEN_SECRET, MAKE_WEBHOOK_URL, etc.) — never commit local env files
.env
.env.local
.env.*.local
php/config.local.php

# Admin CMS local data — credentials, lockout state (never commit real admin data)
admin/data/admin-users.json
admin/data/login-attempts.json

# Netlify/Vercel CLI local project-link cache (created by `netlify dev`/`vercel dev`)
.netlify
.vercel
.remember

# Node (no dependencies are checked in, but ignore in case any get installed locally)
node_modules
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea
