<?php
declare(strict_types=1);

/* Copy this file to config.local.php (gitignored) on hosts where you can't
   set real environment variables (e.g. basic Hostinger shared plans without
   a Node/env-var panel). Apache hosts can instead use `SetEnv` in .htaccess
   and skip this file entirely. */

define('PDF_TOKEN_SECRET', 'replace-with-a-long-random-string');
define('MAKE_WEBHOOK_URL', 'https://hook.eu1.make.com/your-webhook-id');
